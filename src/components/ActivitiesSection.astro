<!-- Activities Background - Makima image, fades in independently -->
<div class="activities__bg" data-activities-bg></div>

<!-- Sky Biomes - 5 different skies that transition between subgroups -->
<div class="activities__void" data-activities-void>
  <!-- Biome 1: Animusic -->
  <div class="activities__sky-biome" data-sky-biome-1>
    <img src="/images/Backgrounds/animusic.png" alt="" class="sky-biome-blur" />
    <img src="/images/Backgrounds/animusic.png" alt="" class="sky-biome-img" />
  </div>
  <!-- Biome 2: Cosplay -->
  <div class="activities__sky-biome" data-sky-biome-2>
    <img src="/images/Backgrounds/cosplay.jpg" alt="" class="sky-biome-blur" />
    <img src="/images/Backgrounds/cosplay.jpg" alt="" class="sky-biome-img" />
  </div>
  <!-- Biome 3: Old Anime -->
  <div class="activities__sky-biome" data-sky-biome-3>
    <img src="/images/Backgrounds/old anime.jpg" alt="" class="sky-biome-blur" />
    <img src="/images/Backgrounds/old anime.jpg" alt="" class="sky-biome-img" />
  </div>
  <!-- Biome 4: Book Club -->
  <div class="activities__sky-biome" data-sky-biome-4>
    <img src="/images/Backgrounds/book club.png" alt="" class="sky-biome-blur" />
    <img src="/images/Backgrounds/book club.png" alt="" class="sky-biome-img" />
  </div>
  <!-- Biome 5: Rainbow -->
  <div class="activities__sky-biome" data-sky-biome-5>
    <img src="/images/Backgrounds/rainbow.png" alt="" class="sky-biome-blur" />
    <img src="/images/Backgrounds/rainbow.png" alt="" class="sky-biome-img" />
  </div>
</div>

<!-- Subgroups Intro Screen - dedicated moment before biomes -->
<div class="subgroups-intro" data-subgroups-intro>
  <div class="subgroups-intro__bg"></div>
  <div class="subgroups-intro__content">
    <h2 class="subgroups-intro__heading" data-subgroups-heading>Subgroups</h2>
    <p class="subgroups-intro__subtitle" data-subgroups-subtitle>Explore Our Communities</p>
    <!-- Mini preview cards with images -->
    <div class="subgroups-intro__previews" data-subgroups-previews>
      <div class="subgroups-intro__card">
        <img src="/images/Backgrounds/animusic.png" alt="Animusic" class="subgroups-intro__card-img" />
        <span class="subgroups-intro__card-name">Animusic</span>
      </div>
      <div class="subgroups-intro__card">
        <img src="/images/Backgrounds/cosplay.jpg" alt="Cosplay" class="subgroups-intro__card-img" />
        <span class="subgroups-intro__card-name">Cosplay</span>
      </div>
      <div class="subgroups-intro__card">
        <img src="/images/Backgrounds/old anime.jpg" alt="Old Anime" class="subgroups-intro__card-img" />
        <span class="subgroups-intro__card-name">Old Anime</span>
      </div>
      <div class="subgroups-intro__card subgroups-intro__card--bookclub">
        <img src="/images/Backgrounds/book club.png" alt="Book Club" class="subgroups-intro__card-img" />
        <span class="subgroups-intro__card-name">Book Club</span>
      </div>
      <div class="subgroups-intro__card">
        <img src="/images/Backgrounds/rainbow.png" alt="Rainbow" class="subgroups-intro__card-img" />
        <span class="subgroups-intro__card-name">Rainbow</span>
      </div>
    </div>
  </div>
</div>

<!-- ===== SUBGROUP 1: ANIMUSIC ===== -->
<div class="subgroup subgroup--animusic" data-subgroup-1>
  <div class="subgroup__left" data-subgroup-1-left>
    <h3 class="subgroup__name subgroup__name--animusic">Animusic</h3>
    <p class="subgroup__tagline">Anime Music & J-Music</p>
  </div>
  <div class="subgroup__right" data-subgroup-1-right>
    <div class="subgroup__info">
      <p class="subgroup__time"><span class="subgroup__label">Meets</span>Saturdays 4-7pm</p>
      <p class="subgroup__leader"><span class="subgroup__label">Leader</span>Annie</p>
    </div>
  </div>
  <!-- Animusic character images -->
  <div class="subgroup__photo subgroup__photo--right" data-subgroup-1-photo-right>
    <img src="/images/Characters/ani1.png" alt="Animusic character" />
  </div>
  <div class="subgroup__photo subgroup__photo--left" data-subgroup-1-photo-left>
    <img src="/images/Characters/ani2.png" alt="Animusic character" />
  </div>
</div>

<!-- ===== SUBGROUP 2: COSPLAY ===== -->
<div class="subgroup subgroup--cosplay" data-subgroup-2>
  <div class="subgroup__left" data-subgroup-2-left>
    <h3 class="subgroup__name subgroup__name--cosplay">Cosplay</h3>
    <p class="subgroup__tagline">Workshops & Events</p>
  </div>
  <div class="subgroup__right" data-subgroup-2-right>
    <div class="subgroup__info">
      <p class="subgroup__leader"><span class="subgroup__label">Leader</span>Nikolai</p>
      <p class="subgroup__desc">A hands on subgroup focused on cosplay workshops and event preparation. Members collaborate on builds, share techniques, and help support major club events like the Maid Café. Check the cosplay channel for updates.</p>
    </div>
  </div>
  <!-- Cosplay photos -->
  <div class="subgroup__photo subgroup__photo--left" data-subgroup-2-photo-left>
    <img src="/images/Photos/Cosplay 1.webp" alt="Cosplay photo" />
  </div>
  <div class="subgroup__photo subgroup__photo--right" data-subgroup-2-photo-right>
    <img src="/images/Photos/Cosplay 2.webp" alt="Cosplay photo" />
  </div>
</div>

<!-- ===== SUBGROUP 3: OLD ANIME ===== -->
<div class="subgroup subgroup--oldanime" data-subgroup-3>
  <div class="subgroup__left" data-subgroup-3-left>
    <h3 class="subgroup__name subgroup__name--oldanime">Old Anime</h3>
    <p class="subgroup__tagline">Classic & Retro</p>
  </div>
  <div class="subgroup__right" data-subgroup-3-right>
    <div class="subgroup__info">
      <p class="subgroup__leader"><span class="subgroup__label">Leader</span>Dylan</p>
      <p class="subgroup__desc">A small group for long time anime fans interested in classic series and deeper discussion. Open to anyone who wants to learn more about older anime and its influence.</p>
    </div>
  </div>
  <!-- Old Anime character images -->
  <div class="subgroup__photo subgroup__photo--right" data-subgroup-3-photo-right>
    <img src="/images/Characters/old1.png" alt="Old anime character" />
  </div>
  <div class="subgroup__photo subgroup__photo--left" data-subgroup-3-photo-left>
    <img src="/images/Characters/old2.png" alt="Old anime character" />
  </div>
</div>

<!-- ===== SUBGROUP 4: BOOK CLUB ===== -->
<div class="subgroup subgroup--bookclub" data-subgroup-4>
  <div class="subgroup__left" data-subgroup-4-left>
    <h3 class="subgroup__name subgroup__name--bookclub">Book Club</h3>
    <p class="subgroup__tagline">Manga & Light Novels</p>
  </div>
  <div class="subgroup__right" data-subgroup-4-right>
    <div class="subgroup__info">
      <p class="subgroup__leader"><span class="subgroup__label">Leader</span>Joseph</p>
      <p class="subgroup__desc">A relaxed subgroup for manga, light novels, and anime related reading. Flexible and open to growth. If you are interested in joining or helping lead, contact Joseph.</p>
    </div>
  </div>
</div>

<!-- ===== SUBGROUP 5: RAINBOW ===== -->
<div class="subgroup subgroup--rainbow" data-subgroup-5>
  <div class="subgroup__left" data-subgroup-5-left>
    <h3 class="subgroup__name subgroup__name--rainbow">Rainbow</h3>
    <p class="subgroup__tagline">LGBTQ+ Community</p>
  </div>
  <div class="subgroup__right" data-subgroup-5-right>
    <div class="subgroup__info">
      <p class="subgroup__leader"><span class="subgroup__label">Leader</span>Corvidae</p>
      <p class="subgroup__desc">A welcoming space for LGBTQ+ anime fans and allies. Open to everyone and focused on community connection, representation, and discussion.</p>
    </div>
  </div>
</div>

<!-- Activities Content -->
<div class="activities-content" data-activities-content>
  <!-- Chainsaw Man character - slides in from bottom-right -->
  <div class="activities__character" data-activities-character>
    <img src="/images/Characters/chainsawman.png" alt="Chainsaw Man" class="activities__character-image" />
  </div>

  <!-- Power character - bottom right of grid -->
  <div class="activities__power" data-activities-power>
    <img src="/images/Characters/power.png" alt="Power" class="activities__power-image" />
  </div>

  <div class="activities__main">
    <h1 class="activities__title" data-activities-title>Activities</h1>
    <p class="activities__description" data-activities-description>From anime themed Jeopardy and movie screenings to creating your own anime dubs, the Terrapin Anime Society offers a wide range of fun activities for people with all kinds of interests. All activities are announced in advance on our Discord!</p>
  </div>

  <!-- Card pile - cards slide in one by one as user scrolls -->
  <div class="activities__card-pile">
    <div class="activities__card card--bingo" data-activity-card>
      <h3 class="activities__card-name">Movie Bingo</h3>
      <p class="activities__card-desc">Create your own bingo cards filled with anime tropes, clichés, and common story beats. As we watch a movie together, players mark off predictions as they happen.</p>
    </div>
    <div class="activities__card card--jeopardy" data-activity-card>
      <h3 class="activities__card-name">Anime Jeopardy</h3>
      <p class="activities__card-desc">A fast-paced trivia game featuring anime-themed questions across different categories and difficulty levels. Compete in teams to test your knowledge of series, characters, studios, openings, and more.</p>
    </div>
    <div class="activities__card card--pictionary" data-activity-card>
      <h3 class="activities__card-name">Charades &amp; Picture Guessing</h3>
      <p class="activities__card-desc">Guess anime characters, scenes, or events through charades, drawings, or images. This game focuses on teamwork, quick thinking, and recognizing iconic moments from across anime history.</p>
    </div>
    <div class="activities__card card--questions" data-activity-card>
      <h3 class="activities__card-name">20 Questions</h3>
      <span class="activities__card-feat">Anime Edition</span>
      <p class="activities__card-desc">Players try to guess the mystery anime, character, or concept by asking yes-or-no questions. A simple but competitive game that rewards clever questions and deep fandom knowledge.</p>
    </div>
    <div class="activities__card card--hottakes" data-activity-card>
      <h3 class="activities__card-name">Hot Takes Night</h3>
      <p class="activities__card-desc">Members share their most controversial anime opinions, from overrated series to unpopular character rankings. After each take, the group votes and discusses, keeping the debate fun and engaging.</p>
    </div>
    <div class="activities__card card--magicboy" data-activity-card>
      <h3 class="activities__card-name">Magic Boy Screening</h3>
      <span class="activities__card-feat">feat. Professor Stevens</span>
      <p class="activities__card-desc">A special screening of Magic Boy, often considered one of the first anime films shown in the United States. Includes historical context led by Professor Stevens.</p>
    </div>
    <div class="activities__card card--chainsaw" data-activity-card>
      <h3 class="activities__card-name">Chainsaw Man Event</h3>
      <span class="activities__card-feat">Sony Partnership</span>
      <p class="activities__card-desc">A large-scale themed event held in partnership with Sony to promote the Chainsaw Man: Reze movie. Featuring exclusive posters, merchandise giveaways, and themed games.</p>
    </div>
    <div class="activities__card card--scavenger" data-activity-card>
      <h3 class="activities__card-name">Anime Scavenger Hunt</h3>
      <p class="activities__card-desc">Anime-themed questions and clues are scattered across multiple subjects. Teams work together to solve prompts and track down answers, blending anime knowledge with creative problem-solving.</p>
    </div>
    <div class="activities__card card--fandubs" data-activity-card>
      <h3 class="activities__card-name">Live Fandubs</h3>
      <p class="activities__card-desc">Members dub over anime scenes in real time while the group watches. A lighthearted event that encourages creativity, voice acting, and humor.</p>
    </div>
    <div class="activities__card card--presentations" data-activity-card>
      <h3 class="activities__card-name">Make Your Own Presentation</h3>
      <p class="activities__card-desc">Create and present a short presentation on any anime-related topic of your choice. Topics range from favorite series and characters to themes, studios, history, or personal interpretations.</p>
    </div>
  </div>
</div>

<style>
  .activities__bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    background-image: url('/images/Backgrounds/makima.webp');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    z-index: 25;
    will-change: opacity;
    backface-visibility: hidden;
    transform: translateZ(0);
  }

  .activities__bg::after {
    content: '';
    position: absolute;
    inset: 0;
    background:
      linear-gradient(rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.4)),
      radial-gradient(ellipse at center, transparent 20%, rgba(0, 0, 0, 0.5) 70%, rgba(0, 0, 0, 0.85) 100%),
      linear-gradient(to top, rgba(0, 0, 0, 0.7) 0%, transparent 40%),
      linear-gradient(to bottom, rgba(0, 0, 0, 0.4) 0%, transparent 30%);
    pointer-events: none;
  }

  .activities-content {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100vh;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    overflow: visible;
    z-index: 30;
    will-change: transform, opacity;
    backface-visibility: hidden;
    transform: translateZ(0);
  }

  .activities__character {
    position: absolute;
    right: 0;
    bottom: 0;
    z-index: 2;
    pointer-events: none;
    will-change: transform;
    backface-visibility: hidden;
  }

  .activities__character-image {
    height: 85vh;
    width: auto;
    max-width: 45vw;
    object-fit: contain;
    object-position: bottom right;
    display: block;
  }

  .activities__power {
    position: absolute;
    left: 0;
    bottom: 0;
    z-index: 2;
    pointer-events: none;
    will-change: transform;
    backface-visibility: hidden;
  }

  .activities__power-image {
    height: 45vh;
    width: auto;
    max-width: 20vw;
    object-fit: contain;
    object-position: bottom left;
    display: block;
  }

  .activities__main {
    position: relative;
    z-index: 5;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: flex-start;
    padding: var(--page-padding, 3rem);
    padding-top: clamp(3rem, 8vh, 6rem);
  }

  .activities__title {
    font-family: 'Inter', sans-serif;
    font-size: clamp(48px, 10vw, 140px);
    font-weight: 800;
    color: #ffffff;
    text-transform: uppercase;
    letter-spacing: -0.04em;
    line-height: 0.9;
    margin: 0;
    text-shadow: 0 4px 40px rgba(0, 0, 0, 0.9), 0 2px 10px rgba(0, 0, 0, 0.7);
  }

  .activities__description {
    font-family: 'Inter', sans-serif;
    font-size: clamp(16px, 1.5vw, 22px);
    font-weight: 400;
    color: rgba(255, 255, 255, 0.9);
    max-width: 550px;
    line-height: 1.4;
    margin: 1.5rem 0 0;
    text-shadow: 0 2px 15px rgba(0, 0, 0, 0.8);
  }

  /* ===== CARD PILE ===== */
  .activities__card-pile {
    position: absolute;
    left: calc(var(--page-padding, 3rem) + 6vw);
    top: clamp(28rem, 52vh, 38rem);
    width: 500px;
    height: 280px;
    z-index: 4;
    pointer-events: none;
  }

  .activities__card {
    position: absolute;
    width: 500px;
    height: 280px;
    background: #fff;
    border: 4px solid #000;
    border-radius: 0;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    gap: 0.5rem;
    padding: 2rem 2rem 2rem 2rem;
    will-change: transform;
    backface-visibility: hidden;
    overflow: hidden;
    box-shadow:
      6px 6px 0 #000,
      inset 0 0 0 2px #000;
  }

  /* Screentone halftone dot pattern - classic manga shading */
  .activities__card::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: radial-gradient(circle, #000 1px, transparent 1px);
    background-size: 6px 6px;
    opacity: 0.12;
    pointer-events: none;
    z-index: 0;
  }

  /* Speed lines radiating from corner - manga action effect */
  .activities__card::after {
    content: '';
    position: absolute;
    top: -50%;
    right: -50%;
    width: 100%;
    height: 100%;
    background: repeating-linear-gradient(
      -45deg,
      transparent,
      transparent 2px,
      rgba(0, 0, 0, 0.03) 2px,
      rgba(0, 0, 0, 0.03) 4px
    );
    pointer-events: none;
    z-index: 0;
  }

  .activities__card-name {
    position: relative;
    z-index: 2;
    font-family: 'Inter', sans-serif;
    font-size: 1.6rem;
    font-weight: 900;
    color: #000;
    text-align: left;
    letter-spacing: -0.02em;
    text-transform: uppercase;
    margin: 0;
    line-height: 1;
    padding: 0.2rem 0 0.4rem;
    border-bottom: 3px solid #000;
    text-shadow: 1px 1px 0 rgba(0,0,0,0.1);
  }

  .activities__card-feat {
    position: relative;
    z-index: 2;
    font-family: 'Inter', sans-serif;
    font-size: 0.7rem;
    font-weight: 900;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #fff;
    background: #000;
    padding: 4px 10px;
    margin: 0.25rem 0 0;
    line-height: 1.2;
  }

  .activities__card-desc {
    position: relative;
    z-index: 2;
    font-family: 'Inter', sans-serif;
    font-size: 0.85rem;
    font-weight: 600;
    color: #000;
    text-align: left;
    line-height: 1.45;
    margin: 0.5rem 0 0;
    max-width: 420px;
  }

  @media (max-width: 768px) {
    .activities__character-image {
      height: 60vh;
      max-width: 60vw;
    }

    .activities__title {
      font-size: clamp(36px, 12vw, 80px);
    }

    .activities__card-pile {
      left: var(--page-padding, 3rem);
      top: clamp(20rem, 40vh, 28rem);
      width: 320px;
      height: 200px;
    }

    .activities__card {
      width: 320px;
      height: 200px;
      padding: 1.5rem;
      border-radius: 0;
    }

    .activities__card-name {
      font-size: 1.1rem;
    }

    .activities__card-feat {
      font-size: 0.65rem;
    }

    .activities__card-desc {
      font-size: 0.8rem;
      line-height: 1.4;
    }
  }

  @media (min-width: 2500px) {
    .activities__title {
      font-size: clamp(140px, 8vw, 240px);
    }

    .activities__character-image {
      height: 90vh;
      max-width: 40vw;
    }

    .activities__card-pile {
      width: 620px;
      height: 360px;
    }

    .activities__card {
      width: 620px;
      height: 360px;
      padding: 3rem;
      border-radius: 0;
    }

    .activities__card-name {
      font-size: 2rem;
    }

    .activities__card-feat {
      font-size: 1rem;
    }

    .activities__card-desc {
      font-size: 1.25rem;
    }
  }

  /* ===== SKY BIOMES CONTAINER ===== */
  .activities__void {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    z-index: 35;
    pointer-events: none;
    will-change: opacity;
    overflow: hidden;
  }

  /* Base biome layer */
  .activities__sky-biome {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    background: #0a0a18; /* Dark fallback */
    overflow: hidden;
  }

  /* First biome starts hidden - intro screen shows first */
  .activities__sky-biome:first-child {
    opacity: 0;
  }

  /* Blurred background layer - fills gaps smoothly */
  .sky-biome-blur {
    position: absolute;
    inset: -20px; /* Extend beyond edges to prevent blur gaps */
    width: calc(100% + 40px);
    height: calc(100% + 40px);
    object-fit: cover;
    object-position: center;
    filter: blur(30px) brightness(0.7);
    transform: scale(1.1);
    z-index: 1;
  }

  /* Main image - sharp, centered, with soft edge blending */
  .sky-biome-img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: contain;
    object-position: center;
    z-index: 2;
    /* Ultra-smooth edge mask - very gradual fade into blurred background */
    -webkit-mask-image: radial-gradient(ellipse 75% 85% at center, black 30%, rgba(0,0,0,0.8) 50%, rgba(0,0,0,0.4) 70%, transparent 95%);
    mask-image: radial-gradient(ellipse 75% 85% at center, black 30%, rgba(0,0,0,0.8) 50%, rgba(0,0,0,0.4) 70%, transparent 95%);
  }

  /* ===== SUBGROUPS INTRO SCREEN - Clean Apple-style ===== */
  .subgroups-intro {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    z-index: 46;
    opacity: 0;
    pointer-events: none;
  }

  .subgroups-intro__bg {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(to bottom, rgba(10, 10, 18, 0.85) 0%, rgba(10, 10, 18, 0.7) 50%, rgba(10, 10, 18, 0.85) 100%),
      url('/images/Backgrounds/subgroup.jpg') center/cover no-repeat;
  }

  .subgroups-intro__content {
    position: absolute;
    top: 45%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    width: 90%;
    max-width: 1200px;
  }

  .subgroups-intro__heading {
    font-family: 'Inter', sans-serif;
    font-size: clamp(48px, 10vw, 140px);
    font-weight: 800;
    letter-spacing: -0.03em;
    line-height: 1;
    margin: 0;
    color: #ffffff;
    text-shadow: 0 4px 40px rgba(0, 0, 0, 0.9), 0 2px 10px rgba(0, 0, 0, 0.7);
  }

  .subgroups-intro__subtitle {
    font-family: 'Inter', sans-serif;
    font-size: clamp(12px, 1.5vw, 20px);
    font-weight: 500;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.5);
    margin-top: 1rem;
  }

  /* Mini preview cards - Apple style */
  .subgroups-intro__previews {
    display: flex;
    justify-content: center;
    gap: clamp(0.75rem, 2vw, 1.5rem);
    margin-top: clamp(2rem, 5vh, 4rem);
    flex-wrap: wrap;
  }

  .subgroups-intro__card {
    position: relative;
    width: clamp(120px, 16vw, 200px);
    height: clamp(80px, 10vw, 130px);
    border-radius: 16px;
    overflow: hidden;
    background: #1a1a2e;
    border: 1px solid rgba(255, 255, 255, 0.15);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .subgroups-intro__card:hover {
    transform: scale(1.05);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.5);
  }

  .subgroups-intro__card-img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .subgroups-intro__card-name {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.5rem 0.75rem;
    font-family: 'Inter', sans-serif;
    font-size: clamp(10px, 1.1vw, 14px);
    font-weight: 600;
    color: #ffffff;
    letter-spacing: 0.02em;
    background: linear-gradient(to top, rgba(0, 0, 0, 0.8) 0%, rgba(0, 0, 0, 0.4) 60%, transparent 100%);
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
  }

  /* Book club image has black bars - zoom in to fill */
  .subgroups-intro__card--bookclub .subgroups-intro__card-img {
    transform: scale(1.4);
    object-position: center center;
  }

  /* ===== SUBGROUP CONTENT - Title top-left, info bottom-right ===== */
  .subgroup {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    z-index: 42;
    pointer-events: none;
    opacity: 0;
    display: block;
    padding: 5vh 4vw;
    overflow: visible;
  }

  .subgroup__left,
  .subgroup__right {
    max-width: 500px;
    will-change: transform, opacity;
    overflow: visible;
    position: absolute;
    /* Apple-style frosted glass */
    background: rgba(0, 0, 0, 0.35);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
    backdrop-filter: blur(20px) saturate(180%);
    padding: 2rem 3.5rem;
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow:
      0 8px 32px rgba(0, 0, 0, 0.3),
      inset 0 1px 0 rgba(255, 255, 255, 0.1);
  }

  .subgroup__left {
    text-align: left;
    top: 5vh;
    left: 12vw;
  }

  .subgroup__right {
    text-align: right;
    bottom: 5vh;
    right: 12vw;
  }

  .subgroup__name {
    font-family: 'Inter', sans-serif;
    font-size: clamp(36px, 5vw, 72px);
    font-weight: 900;
    letter-spacing: -0.02em;
    line-height: 1.3;
    margin: 0 0 0.5rem;
    text-transform: uppercase;
    padding: 0.2em 0.1em;
  }

  .subgroup__tagline {
    font-family: 'Inter', sans-serif;
    font-size: clamp(12px, 1.2vw, 18px);
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin: 0;
    opacity: 0.8;
  }

  .subgroup__info {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .subgroup__label {
    display: block;
    font-size: clamp(10px, 0.9vw, 14px);
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    opacity: 0.6;
    margin-bottom: 0.2rem;
  }

  .subgroup__time,
  .subgroup__contact,
  .subgroup__leader {
    font-family: 'Inter', sans-serif;
    font-size: clamp(14px, 1.3vw, 20px);
    font-weight: 600;
    margin: 0;
    line-height: 1.3;
  }

  .subgroup__channel {
    font-family: 'Inter', sans-serif;
    font-size: clamp(11px, 1vw, 15px);
    font-weight: 500;
    margin: 0;
    opacity: 0.7;
  }

  .subgroup__desc {
    font-family: 'Inter', sans-serif;
    font-size: clamp(12px, 1.1vw, 16px);
    font-weight: 500;
    line-height: 1.5;
    margin: 0.5rem 0 0;
    opacity: 0.85;
  }

  /* ===== SUBGROUP COLOR THEMES ===== */

  /* Animusic - Pink/Cyan */
  .subgroup--animusic .subgroup__name {
    background: linear-gradient(135deg, #ff6ec7 0%, #00d4ff 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .subgroup--animusic .subgroup__tagline,
  .subgroup--animusic .subgroup__info {
    color: #ffffff;
    text-shadow: 0 2px 20px rgba(0,0,0,0.5);
  }

  /* Cosplay - Warm Orange/Gold */
  .subgroup--cosplay .subgroup__name {
    background: linear-gradient(135deg, #ff9f43 0%, #ffd32a 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .subgroup--cosplay .subgroup__tagline,
  .subgroup--cosplay .subgroup__info {
    color: #fff8e8;
    text-shadow: 0 2px 20px rgba(0,0,0,0.5);
  }

  /* Cosplay photos - top right and bottom left */
  .subgroup__photo {
    position: absolute;
    z-index: 41;
    pointer-events: none;
  }
  .subgroup__photo img {
    height: clamp(200px, 38vh, 450px);
    width: auto;
    object-fit: contain;
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.5);
  }
  .subgroup__photo--left {
    left: 12vw;
    bottom: 5vh;
  }
  .subgroup__photo--right {
    right: 12vw;
    top: 5vh;
  }

  /* Old Anime - White/Gray */
  .subgroup--oldanime .subgroup__name {
    color: #ffffff;
    text-shadow: 2px 2px 0 #000, -2px -2px 0 #000, 2px -2px 0 #000, -2px 2px 0 #000;
  }
  .subgroup--oldanime .subgroup__tagline,
  .subgroup--oldanime .subgroup__info {
    color: #e0e0e0;
    text-shadow: 1px 1px 0 #000;
  }

  /* Book Club - Warm Purple */
  .subgroup--bookclub .subgroup__name {
    background: linear-gradient(135deg, #e8b4f8 0%, #a855f7 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .subgroup--bookclub .subgroup__tagline,
  .subgroup--bookclub .subgroup__info {
    color: #f0e0ff;
    text-shadow: 0 2px 20px rgba(0,0,0,0.5);
  }

  /* Rainbow - Pride Gradient */
  .subgroup--rainbow .subgroup__name {
    background: linear-gradient(135deg,
      #e74c3c 0%, #e67e22 20%, #f1c40f 40%,
      #2ecc71 60%, #3498db 80%, #9b59b6 100%
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .subgroup--rainbow .subgroup__tagline,
  .subgroup--rainbow .subgroup__info {
    color: #ffffff;
    text-shadow: 0 2px 20px rgba(0,0,0,0.6);
  }

  @media (max-width: 768px) {
    .subgroups-title {
      top: 5%;
    }

    .subgroups-title__heading {
      font-size: clamp(32px, 10vw, 60px);
    }

    .subgroup {
      flex-direction: column;
      justify-content: flex-start;
      padding: 15vh 1.5rem 0;
      gap: 1rem;
    }

    .subgroup__left,
    .subgroup__right {
      max-width: 100%;
      text-align: center;
    }

    .subgroup__name {
      font-size: clamp(28px, 8vw, 48px);
      padding: 0.15em 0;
    }

    .subgroup__left,
    .subgroup__right {
      padding: 1.5rem 1.75rem;
    }

    .subgroup__info {
      align-items: center;
    }
  }

  @media (min-width: 2500px) {
    .subgroups-title__heading {
      font-size: clamp(120px, 6vw, 180px);
    }

    .subgroup__left,
    .subgroup__right {
      max-width: 450px;
    }

    .subgroup__name {
      font-size: clamp(72px, 4vw, 100px);
    }

    .subgroup__desc {
      font-size: clamp(16px, 1vw, 22px);
    }
  }
</style>
